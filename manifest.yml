---
name: VS Code Tunnel
category: Interactive Apps
subcategory: ""
role: batch_connect
icon: fa://code
description: |
  Launch a VS Code Remote Tunnel inside a Slurm job on a compute node.

  - The browser view shows tunnel status and login output.
  - Connect from your own local VS Code via Remote Tunnels.
  - Closing the browser does not stop the Slurm job.
